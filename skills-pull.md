Claude 스킬 관련 폴더들을 git pull 해줘.

대상 폴더:
- ~/.claude/commands
- ~/Documents/claude_skills

## 처리 흐름

각 폴더에 대해:
1. `git -C {폴더} pull` 실행
2. 결과 요약 (Already up to date / 업데이트된 파일 목록)

완료 후 두 폴더의 결과를 한국어로 요약해서 보여줘.
